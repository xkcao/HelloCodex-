# Soccer Dashboard

This project contains the existing HelloCodex European soccer dashboard.

## Structure

- `index.html` — project page
- `css/styles.css` — project styling
- `js/app.js` — rendering and interactions
- `data/soccer.json` — soccer data used by the dashboard

The project is intentionally self-contained so it can evolve independently from other HelloCodex projects.
